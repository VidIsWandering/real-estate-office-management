# ----- Database (Local Docker) -----
# Các giá trị này phải khớp với 'environment' của service 'db' trong docker-compose.yml
DATABASE_URL="postgresql://devuser:devpassword@db:5432/se100_dev_db"

# ----- Cloudinary (File Storage) -----
# Lấy các giá trị này từ tài khoản Cloudinary của bạn (Task 1.2)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ----- JWT Secret (Bảo mật) -----
# Cần một chuỗi ký tự ngẫu nhiên, phức tạp để mã hóa token
# Ví dụ: openssl rand -base64 32
JWT_SECRET=your_super_secret_key_here

# ----- Server Port -----
# Cổng mà backend sẽ chạy bên trong container (phải khớp với EXPOSE trong Dockerfile)
PORT=8080